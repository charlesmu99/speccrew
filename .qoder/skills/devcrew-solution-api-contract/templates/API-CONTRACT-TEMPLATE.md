# 接口契约 - [功能名称]

> 基于 Solution：[链接到对应 Solution 文件]
>
> **重要**：本文档由 Solution Agent 输出，是前后端协作的唯一基准。
> 设计/开发阶段**只读引用，不得修改**。如需变更，必须回溯至 Solution Agent 修正后重新确认。

---

## 变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|----------|--------|
| v1.0 | [日期] | 初始版本 | Solution Agent |

---

## 接口清单

| 接口名称 | 方法 | URL | 说明 |
|----------|------|-----|------|
| [接口1] | POST | `/api/v1/xxx` | [说明] |
| [接口2] | GET | `/api/v1/xxx/:id` | [说明] |

---

## 接口详情

### [接口名称]

**基本信息**

| 项 | 内容 |
|----|------|
| 方法 | `POST` |
| URL | `/api/v1/xxx` |
| 描述 | [接口描述] |
| 认证 | 需要 / 不需要 |

**请求头**

| 字段 | 值 | 说明 |
|------|----|------|
| `Content-Type` | `application/json` | |
| `Authorization` | `Bearer {token}` | 需要认证时 |

**请求参数**

| 字段 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| [字段] | string | 是 | [说明] | `"示例值"` |
| [字段] | int | 否 | [说明] | `1` |

**请求示例**

```json
{
  "[字段]": "[值]"
}
```

**响应结构**

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | int | 0=成功，非0=失败 |
| `message` | string | 结果描述 |
| `data` | object | 响应数据 |
| `data.[字段]` | [类型] | [说明] |

**响应示例（成功）**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "[字段]": "[值]"
  }
}
```

**错误码**

| 错误码 | HTTP 状态 | 说明 | 处理建议 |
|--------|-----------|------|----------|
| 4001 | 400 | 参数校验失败 | 检查请求参数 |
| 4003 | 403 | 无权限 | 检查用户权限 |
| 4004 | 404 | 资源不存在 | 确认资源ID |
| 5000 | 500 | 服务内部错误 | 联系后端排查 |

---

**契约状态：** ⬜ 草稿 / ⬜ 评审中 / ⬜ 已确认
**确认日期：** [日期]
**确认人：** [姓名]
